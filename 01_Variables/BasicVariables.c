#include <stdio.h>

int main ()
{
    printf("The size of the integer is %ld bytes\n",sizeof(int));
    printf("The size of the float is %ld bytes\n",sizeof(float));
    printf("The size of the long integer is %ld bytes\n",sizeof(long));
}